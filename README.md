# docform-core
The backend modular monolith — everything from PRD.md/ARCHITECTURE.md except the contract itself: auth, users, doctors, patients, availability, appointments, referrals, consultations, prescriptions, lab-orders, facilities, notifications, and a payments module that imports docforum-escrow as a dependency. Owns Postgres/Prisma, owns all PHI.
