# events

Placeholder for internal domain events (e.g. `referral.created`,
`order.fulfilled`) if/when the notification module needs to react to
cross-module state changes without a direct service call. Not yet designed —
introduce only when a concrete need appears (see AGENTS.md working style:
prefer boring/minimal solutions).
